# hotkey deamon
#

# terminal emulator
super + Return
	rxvt

# browser
super + f
	firefox

# launcher
alt + space
	urxvt -title "run" -e "zsh" -c "berryc window_center; sort <(print -rl -- $\{(ko)commands\}) | fzf | (nohup $SHELL &) > /dev/null 2>&1"

# Alternative: as script to pipe into (more versatile)
# urxvt -title "fmenu" -e "$SHELL" -c "fzf $* < /proc/$$/fd/0 > /proc/$$/fd/1"

XF86Audio{Play,Stop,Next,Prev}
	cmus-remote --{pause,stop,next,prev}

alt + Print
	scrot -e 'mv $f ~/desk/'

#
# berry hotkeys
#

super + alt + {Left,Up,Right}
	berryc {snap_left,window_monocle,snap_right}

alt + q
	berryc window_close

alt + Tab
	bash $HOME/.config/berry/helpers/switcher.sh

~button1
	berryc pointer_focus
